# auto_landing_quadraceptor

The drone will starts flying untel you press Q button, the drone will starts looking for safe area to landing there

## unreal engine settings
1) engine 5.8.0
2) download citySample 

## Python settings:
1) create AirSim_env 
```bash
python -m venv AirSim_env
```
2) install requiremnt ,,
```bash
pip install -r requirements.txt
```
